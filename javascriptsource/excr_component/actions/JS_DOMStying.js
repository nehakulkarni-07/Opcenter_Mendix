// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
import "mx-global";
import { Big } from "big.js";

// BEGIN EXTRA CODE
// END EXTRA CODE

/**
 * @returns {Promise.<void>}
 */
export async function JS_DOMStying() {
	// BEGIN USER CODE
    (function refinedTwoBlockCentered() {
    function applyRefinedBlocks(selector = '.nodeText') {
        document.querySelectorAll(selector).forEach(node => {
            if (node.dataset.processed) return;
            const text = node.textContent;
            if (!text.includes('\\n')) return;

            const lines = text.split('\\n');
            const baseX = parseFloat(node.getAttribute('x') || '10');
            const baseY = parseFloat(node.getAttribute('y') || '0');
            const lineHeight = 18;
            const paddingX = 10;
            const paddingY = 6;
            const radius = 4;

            const estimateTextWidth = (text) => text.length * 7.5;
            const textWidth = Math.max(...lines.map(line => estimateTextWidth(line)));
            const boxWidth = textWidth + paddingX * 2;
            const blockCenterX = baseX; // Keep baseX as logical center

            const group = document.createElementNS('http://www.w3.org/2000/svg', 'g');

            // Top rectangle (header, rounded top corners only)
            const topRect = document.createElementNS('http://www.w3.org/2000/svg', 'path');
            const topY = baseY - lineHeight;
            topRect.setAttribute('d',
                `M${blockCenterX - boxWidth / 2 + radius},${topY}
                 h${boxWidth - radius * 2}
                 a${radius},${radius} 0 0 1 ${radius},${radius}
                 v${lineHeight - radius}
                 h-${boxWidth}
                 v-${lineHeight - radius}
                 a${radius},${radius} 0 0 1 ${radius},-${radius}
                 z`);
            topRect.setAttribute('fill', '#d0e7ff');
            topRect.setAttribute('stroke', '#000');

            // Bottom rectangle (body, rounded bottom corners only)
            const bottomRect = document.createElementNS('http://www.w3.org/2000/svg', 'path');
            const bottomY = baseY;
            bottomRect.setAttribute('d',
                `M${blockCenterX - boxWidth / 2},${bottomY}
                 h${boxWidth}
                 v${lineHeight - radius}
                 a${radius},${radius} 0 0 1 -${radius},${radius}
                 h-${boxWidth - radius * 2}
                 a${radius},${radius} 0 0 1 -${radius},-${radius}
                 v-${lineHeight - radius}
                 z`);
            bottomRect.setAttribute('fill', '#fff');
            bottomRect.setAttribute('stroke', '#000');

            group.appendChild(topRect);
            group.appendChild(bottomRect);

            // Create centered text
            const textElement = document.createElementNS('http://www.w3.org/2000/svg', 'text');
            textElement.setAttribute('x', blockCenterX);
            textElement.setAttribute('text-anchor', 'middle');
            textElement.setAttribute('font-size', '14px');

            // First line (header)
            const tspan1 = document.createElementNS('http://www.w3.org/2000/svg', 'tspan');
            tspan1.setAttribute('x', blockCenterX);
            tspan1.setAttribute('y', baseY - (lineHeight / 2) + 4); // adjust for visual balance
            tspan1.textContent = lines[0].trim();

            // Second line (body)
            const tspan2 = document.createElementNS('http://www.w3.org/2000/svg', 'tspan');
            tspan2.setAttribute('x', blockCenterX);
            tspan2.setAttribute('y', baseY + (lineHeight / 2) + 4); // adjust for visual balance
            tspan2.textContent = lines[1].trim();

            textElement.appendChild(tspan1);
            textElement.appendChild(tspan2);

            group.appendChild(textElement);

            node.parentNode.insertBefore(group, node);
            node.remove();
            group.dataset.processed = 'true';
        });
    }

    applyRefinedBlocks();

    const observer = new MutationObserver(() => applyRefinedBlocks());
    observer.observe(document.body, { childList: true, subtree: true });
})();




	// END USER CODE
}
