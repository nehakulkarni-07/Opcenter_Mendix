// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
import "mx-global";
import { Big } from "big.js";

// BEGIN EXTRA CODE
// END EXTRA CODE

/**
 * @param {Nanoflow} [openPage]
 * @returns {Promise.<void>}
 */
export async function JS_AddEventListenerOnSVGNodes_Custom(openPage) {
	// BEGIN USER CODE

	function initialize() {
	// console.log('test refresh')	
    const elements = document.querySelectorAll('[id*="-button"]');

    elements.forEach(element => {
        // Check if the listener was already added
        if (!element.dataset.listenerAdded) {
            element.addEventListener('click', function () {
                openPage({ 'NodeDetails': element.id }); // Customize 'xxx' based on clicked element if needed
            });
            element.dataset.listenerAdded = 'true'; // Mark it as having a listener
			element.style.cursor = 'pointer'; //fix (6/20/2025)
        	}
    	});
	}

// Don't invoke immediately, just pass the function
console.log('test refresh5')
	if (document.readyState === 'loading') {
    	document.addEventListener('DOMContentLoaded', initialize);
	//	console.log('test refresh2')	
	} else {
	//	console.log('test refresh4')	
    	initialize();
	//	console.log('test refresh3')	
	}

	// Run every second to catch dynamic changes
	setInterval(initialize, 1000);
	// END USER CODE
}
