// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
import "mx-global";
import { Big } from "big.js";

// BEGIN EXTRA CODE
// END EXTRA CODE

/**
 * @param {string} listClass - css for the list class
 * @param {string} footerClass - css class for the footer/buttons the list should sit on top of
 * @returns {Promise.<void>}
 */
export async function ACT_AdjustLogListHeight(listClass, footerClass) {
	// BEGIN USER CODE
	
		window.alert("success!");
  	var list = document.querySelector("."+ listClass);
	var foot = document.querySelector("."+ footerClass);
	var cont = document.querySelector(".mx-scrollcontainer-middle")
	if(list && foot && cont){
		cont = cont.getBoundingClientRect();
		let offsetH = cont.height; 
		
		let listpos = list.getBoundingClientRect();
		let footpos = foot.getBoundingClientRect();
		
		let startpos = listpos.top + offsetH;
		let endpos = listpos.bottom + footpos.height ;
		if(startpos && endpos){
			window.alert(endpos)
			let height = (endpos - startpos);
			window.alert(height);
			if(height) list.style.maxHeight = height + "px"
		}
	
};
	
	// END USER CODE
}
