// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
import "mx-global";
import { Big } from "big.js";

// BEGIN EXTRA CODE
// END EXTRA CODE

/**
 * @returns {Promise.<string>}
 */
export async function JS_ClearActiveStateOfTextboxElements() {
	// BEGIN USER CODE
	return new Promise(function(resolve, reject) {

		document.querySelectorAll('input.qr-input-active').forEach((e)=>{e.classList.remove('qr-input-active')});
  		document.querySelectorAll('[btn-onchange-active="true"]').forEach((e)=>{e.removeAttribute('btn-onchange-active')});
	
		return resolve("Completed");
	});

	// END USER CODE
}
