// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package saml20.actions;

import com.mendix.core.Core;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import saml20.implementation.metadata.SPMetadataGenerator;
import saml20.implementation.security.CredentialRepository;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import com.mendix.systemwideinterfaces.core.UserAction;

public class CreateSSOMetadataFile extends UserAction<java.lang.Boolean>
{
	/** @deprecated use spMetadata.getMendixObject() instead. */
	@java.lang.Deprecated(forRemoval = true)
	private final IMendixObject __spMetadata;
	private final saml20.proxies.SPMetadata spMetadata;
	/** @deprecated use ssoMetadata.getMendixObject() instead. */
	@java.lang.Deprecated(forRemoval = true)
	private final IMendixObject __ssoMetadata;
	private final saml20.proxies.SSOConfiguration ssoMetadata;

	public CreateSSOMetadataFile(
		IContext context,
		IMendixObject _spMetadata,
		IMendixObject _ssoMetadata
	)
	{
		super(context);
		this.__spMetadata = _spMetadata;
		this.spMetadata = _spMetadata == null ? null : saml20.proxies.SPMetadata.initialize(getContext(), _spMetadata);
		this.__ssoMetadata = _ssoMetadata;
		this.ssoMetadata = _ssoMetadata == null ? null : saml20.proxies.SSOConfiguration.initialize(getContext(), _ssoMetadata);
	}

	@java.lang.Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		if (this.spMetadata == null || this.ssoMetadata == null) {
			return false;
		}
        CredentialRepository credRep = CredentialRepository.getInstance();

        this.spMetadata.setName("sp_metadata.xml");
        try (ByteArrayOutputStream out = SPMetadataGenerator.generate(getContext(), this.__spMetadata,this.ssoMetadata, credRep);
             ByteArrayInputStream inputStream = new ByteArrayInputStream(out.toByteArray())) {
            Core.storeFileDocumentContent(getContext(), this.__spMetadata, inputStream);
        }
        return true;

		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "CreateSSOMetadataFile";
	}

	// BEGIN EXTRA CODE

	// END EXTRA CODE
}
