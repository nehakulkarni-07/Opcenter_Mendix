// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package saml20.actions;

import com.mendix.core.Core;
import com.mendix.logging.ILogNode;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import saml20.implementation.common.Constants;
import saml20.implementation.security.KeyStoreHelper;
import java.io.InputStream;
import java.util.Enumeration;
import com.mendix.systemwideinterfaces.core.UserAction;

public class PreValidateKeyStoreFile extends UserAction<java.lang.Boolean>
{
	/** @deprecated use keyStoreObj.getMendixObject() instead. */
	@java.lang.Deprecated(forRemoval = true)
	private final IMendixObject __keyStoreObj;
	private final saml20.proxies.KeyStore keyStoreObj;
	private final saml20.proxies.EncryptionKeyLength encryptionKeyLength;

	public PreValidateKeyStoreFile(
		IContext context,
		IMendixObject _keyStoreObj,
		java.lang.String _encryptionKeyLength
	)
	{
		super(context);
		this.__keyStoreObj = _keyStoreObj;
		this.keyStoreObj = _keyStoreObj == null ? null : saml20.proxies.KeyStore.initialize(getContext(), _keyStoreObj);
		this.encryptionKeyLength = _encryptionKeyLength == null ? null : saml20.proxies.EncryptionKeyLength.valueOf(_encryptionKeyLength);
	}

	@java.lang.Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		String aliasName = keyStoreObj.getAlias();
		String password = keyStoreObj.getPassword();
		boolean isAliasFindInKeyStore = false;
		if (password != null) {
			password = KeyStoreHelper.decrypt(password, getContext());
		}

		try (InputStream inStr = Core.getFileDocumentContent(this.getContext(), __keyStoreObj)) {
			java.security.KeyStore ks = KeyStoreHelper.getKeystore(inStr, encryptionKeyLength, password);
			Enumeration<String> aliases = ks.aliases();
			while (aliases.hasMoreElements()) {
				String name = aliases.nextElement();
				if (name.equals(aliasName)) {
					isAliasFindInKeyStore = true;
					break;
				}
			}
		} catch (Exception e) {
			Core.getLogger(Constants.LOGNODE).error(e.getMessage());
			return false;
		}
		if (!isAliasFindInKeyStore) {
			Core.getLogger(Constants.LOGNODE).error("Alias is not found in key store file");
		}
		return isAliasFindInKeyStore;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "PreValidateKeyStoreFile";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
