// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package saml20.actions;

import com.mendix.core.Core;
import com.mendix.externalinterface.connector.RequestHandler;
import com.mendix.logging.ILogNode;
import com.mendix.m2ee.api.IMxRuntimeRequest;
import com.mendix.m2ee.api.IMxRuntimeResponse;
import com.mendix.systemwideinterfaces.core.IContext;
import saml20.implementation.common.Constants;
import saml20.implementation.common.HTTPUtils;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.Writer;
import com.mendix.systemwideinterfaces.core.UserAction;

public class SubmitLoginFormJS extends UserAction<java.lang.Boolean>
{
	public SubmitLoginFormJS(IContext context)
	{
		super(context);
	}

	@java.lang.Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		JSRequestHandler rHandler = new JSRequestHandler();
		Core.addRequestHandler(Constants.SAMLAction.SubmitLoginForm.name(), rHandler);
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "SubmitLoginFormJS";
	}

	// BEGIN EXTRA CODE
	public static class JSRequestHandler extends RequestHandler {
		private static ILogNode _logNode = Core.getLogger("JSHandler");
		@Override
		protected void processRequest( IMxRuntimeRequest request, IMxRuntimeResponse response, String path ) throws Exception {
			try{
				HTTPUtils.SubmitLoginForm(response);
			} catch (IOException e) {
				_logNode.error("Unable to render Javascript", e);
			}
		}


	}
	// END EXTRA CODE
}
