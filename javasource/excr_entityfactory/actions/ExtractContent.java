// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package excr_entityfactory.actions;

import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.UserAction;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.core.Core;

public class ExtractContent extends UserAction<java.util.List<IMendixObject>>
{
	private final java.lang.String InputContent;

	public ExtractContent(
		IContext context,
		java.lang.String _inputContent
	)
	{
		super(context);
		this.InputContent = _inputContent;
	}

	@java.lang.Override
	public java.util.List<IMendixObject> executeAction() throws Exception
	{
		// BEGIN USER CODE
		IContext ctx = getContext();
		java.util.List<String> stringList = new java.util.ArrayList<>();
		java.util.List<IMendixObject> returnList = new java.util.ArrayList<>();
		
        if (InputContent == null || InputContent.isEmpty()) {
            return returnList;
        }

        // Regex: match all parenthesized conditions
        //String regex = "\\([^()]+\\)";
        String regex = "\\([^\\(\\)]*(?:\\([^\\(\\)]*\\)[^\\(\\)]*)*\\)";

        java.util.regex.Pattern pattern = java.util.regex.Pattern.compile(regex);
        java.util.regex.Matcher matcher = pattern.matcher(InputContent);

        while (matcher.find()) {
            stringList.add(matcher.group().trim());
        }
		
        for (String s : stringList) {
            IMendixObject javaActionReturnObject = Core.instantiate(ctx, "EXCR_EntityFactory.JavaActionReturn"); 
            javaActionReturnObject.setValue(ctx, "StringValue", s);
            returnList.add(javaActionReturnObject);
        }
        return returnList;
		
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "ExtractContent";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
