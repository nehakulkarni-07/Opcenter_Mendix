// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package documentgeneration.actions;

import java.math.BigDecimal;
import java.math.RoundingMode;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.UserAction;

public class JA_StringToTime extends UserAction<java.lang.String>
{
	private final java.lang.String InputScientificString;

	public JA_StringToTime(
		IContext context,
		java.lang.String _inputScientificString
	)
	{
		super(context);
		this.InputScientificString = _inputScientificString;
	}

	@java.lang.Override
	public java.lang.String executeAction() throws Exception
	{
		// BEGIN USER CODE
		if (this.InputScientificString == null || this.InputScientificString.isEmpty()) {
            return null; 
        }
		try {
			BigDecimal number = new BigDecimal(this.InputScientificString);
        	final int SECONDS_PER_DAY_INT = 86400;
        	BigDecimal totalSecondsDecimal  = number.multiply(new BigDecimal(SECONDS_PER_DAY_INT));
        	BigDecimal roundedSeconds = totalSecondsDecimal.setScale(0, RoundingMode.FLOOR);
        	long totalSeconds = roundedSeconds.longValue();
        
        	long days = totalSeconds / SECONDS_PER_DAY_INT;
        	long hours = (totalSeconds % SECONDS_PER_DAY_INT) / 3600;
        	long minutes = (totalSeconds % 3600) / 60;
        	long seconds = totalSeconds % 60;
        
        	String formattedResult = String.format("%d.%02d:%02d:%02d", days, hours, minutes, seconds);

			return formattedResult;
		} catch (NumberFormatException e) {
			throw new RuntimeException("Error parsing scientific notation string: " + this.InputScientificString, e);
		}
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "JA_StringToTime";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
