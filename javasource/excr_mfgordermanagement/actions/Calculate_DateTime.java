// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package excr_mfgordermanagement.actions;

import com.mendix.systemwideinterfaces.core.IContext;
import java.util.Date; 
import java.time.ZonedDateTime;
import java.time.Instant;
import java.time.Duration;
import java.time.ZoneId;
import java.time.format.DateTimeFormatter;
import java.time.format.DateTimeFormatterBuilder;
import java.text.SimpleDateFormat;
import java.text.ParseException;
import com.mendix.systemwideinterfaces.core.UserAction;

public class Calculate_DateTime extends UserAction<java.lang.String>
{
	private final java.lang.String StartDateTimeUTC;
	private final java.lang.String CompleteDateTimeUTC;
	private final java.lang.String ClientTimezone;
	private final java.lang.String OutputFormat;

	public Calculate_DateTime(
		IContext context,
		java.lang.String _startDateTimeUTC,
		java.lang.String _completeDateTimeUTC,
		java.lang.String _clientTimezone,
		java.lang.String _outputFormat
	)
	{
		super(context);
		this.StartDateTimeUTC = _startDateTimeUTC;
		this.CompleteDateTimeUTC = _completeDateTimeUTC;
		this.ClientTimezone = _clientTimezone;
		this.OutputFormat = _outputFormat;
	}

	@java.lang.Override
	public java.lang.String executeAction() throws Exception
	{
		// BEGIN USER CODE

		 if (this.StartDateTimeUTC != null && !this.StartDateTimeUTC.isEmpty()) {
            DateTimeFormatter formatter = new DateTimeFormatterBuilder().appendPattern("yyyy-MM-dd HH:mm:ss").optionalStart().appendPattern(".SSS").optionalEnd().toFormatter().withZone(ZoneId.of("UTC"));
            ZonedDateTime startDateTimeUTC = ZonedDateTime.parse(this.StartDateTimeUTC, formatter);
            ZonedDateTime startDateTimeInClientZone = startDateTimeUTC.withZoneSameInstant(ZoneId.of(this.ClientTimezone));

            ZonedDateTime completeDateTimeInClientZone;

            if (this.CompleteDateTimeUTC != null && !this.CompleteDateTimeUTC.isEmpty()) {
                ZonedDateTime completeDateTimeUTC = ZonedDateTime.parse(this.CompleteDateTimeUTC, formatter);
                completeDateTimeInClientZone = completeDateTimeUTC.withZoneSameInstant(ZoneId.of(this.ClientTimezone));
            } else {
                completeDateTimeInClientZone = ZonedDateTime.now(ZoneId.of(this.ClientTimezone));
            }

            Duration duration = Duration.between(startDateTimeInClientZone, completeDateTimeInClientZone);

            switch (OutputFormat.toLowerCase()) {
                case "hh:mm":
                    long totalHours = Math.abs(duration.toHours());
                    long totalMinutes = Math.abs(duration.toMinutes() % 60);
                    return String.format("%d:%02d", totalHours, totalMinutes);
                case "dd:hh:mm":
                default:
                    long days = Math.abs(duration.toDays());
                    long hours = Math.abs(duration.toHours() % 24);
                    long minutes = Math.abs(duration.toMinutes() % 60);
                    return String.format("%d days, %d hours, %02d minutes", days, hours, minutes);
            }
        }

        return null;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "Calculate_DateTime";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
