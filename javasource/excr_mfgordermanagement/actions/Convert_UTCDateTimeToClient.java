// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package excr_mfgordermanagement.actions;

import com.mendix.systemwideinterfaces.core.IContext;
import java.util.Date; 
import java.time.ZonedDateTime;
import java.time.Instant;
import java.time.Duration;
import java.time.ZoneId;
import java.time.format.DateTimeFormatter;
import java.time.format.DateTimeFormatterBuilder;
import java.text.SimpleDateFormat;
import java.text.ParseException;
import com.mendix.systemwideinterfaces.core.UserAction;

public class Convert_UTCDateTimeToClient extends UserAction<java.util.Date>
{
	private final java.lang.String DateTimeUTC;
	private final java.lang.String ClientTimezone;

	public Convert_UTCDateTimeToClient(
		IContext context,
		java.lang.String _dateTimeUTC,
		java.lang.String _clientTimezone
	)
	{
		super(context);
		this.DateTimeUTC = _dateTimeUTC;
		this.ClientTimezone = _clientTimezone;
	}

	@java.lang.Override
	public java.util.Date executeAction() throws Exception
	{
		// BEGIN USER CODE

		if (this.DateTimeUTC != null && !this.DateTimeUTC.isEmpty()) {
			//DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS").withZone(ZoneId.of("UTC"));
			DateTimeFormatter formatter = new DateTimeFormatterBuilder().appendPattern("yyyy-MM-dd HH:mm:ss").optionalStart().appendPattern(".SSS").optionalEnd().toFormatter().withZone(ZoneId.of("UTC"));
			ZonedDateTime dateTimeUTC = ZonedDateTime.parse(this.DateTimeUTC, formatter);
			ZonedDateTime dateTimeInClientZone = dateTimeUTC.withZoneSameInstant(ZoneId.of(this.ClientTimezone));
			
			return java.util.Date.from(dateTimeInClientZone.toInstant());
		}

		return null;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 * @return a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "Convert_UTCDateTimeToClient";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
